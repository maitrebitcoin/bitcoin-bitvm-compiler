// test the for statement
int8 main(int8 max_i) 
{
	// return the sum of int form 1 to max_i, up to 10
	int32 sum=0;
	for (int8 i=0;i<10;i++) {
		if (i==max_i)
			return sum;
		sum = sum+i;
	}
	return sum;
}